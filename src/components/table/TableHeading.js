import React from "react";

const TableHeading = ({ title }) => {
  return (
    <h4 className="font-weight-bold" style={{ color: "#ff5e0f" }}>
      {title}
    </h4>
  );
};

export default TableHeading;
